# Airflow Configuration
AIRFLOW_UID=50000

# [CRITICAL] Host path for Docker-in-Docker mounting
# DockerOperator needs to know where the code lives on the HOST machine
# to mount it into the Agent container.
AIRFLOW_HOST_PROJECT_PATH=/Users/jshstorm/Documents/github/agent
